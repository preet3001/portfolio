import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Preet Singh — Flutter Developer",
  description:
    "Flutter Engineer with ~5 years of experience. Built platform modules in Adani One (30M+ users). Now shipping AI-powered edtech at Tutero.",
  openGraph: {
    title: "Preet Singh — Flutter Developer",
    description:
      "Flutter Engineer. Ex-Adani Digital Labs. Building AI-powered edtech at Tutero.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
